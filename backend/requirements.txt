django>=5.1
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.4
treys>=0.1.8
pokerkit>=0.7,<0.9
python-dotenv>=1.0
pytest>=8.0
pytest-django>=4.8
dj-database-url>=2.1
# PostgreSQL driver — the production database target (project.md §3; SQLite is
# local-dev only). The binary wheel needs no local build toolchain.
psycopg[binary]>=3.1

# --- Production serving (not used by `manage.py runserver` in local dev) ---
# WSGI server Render invokes as the start command.
gunicorn>=22.0
# Serves collected static files (Django admin CSS/JS, DRF browsable API) straight
# from the web process, so the demo needs no separate CDN or static host.
whitenoise>=6.7
